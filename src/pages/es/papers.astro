---
import SEO from '../../components/SEO.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

type PaperItem = {
	title: string;
	date: string;
	status: string;
	description: string;
	reviewStatus: string;
};

const papers: PaperItem[] = [
	{
		title: 'Cuando el Éxito Oculta la Dependencia',
		date: 'Agosto de 2026',
		status: 'Enviado',
		description:
			'Cómo un sistema que funciona puede hacer más difícil saber qué pasaría si se retiraran las salvaguardas que sostienen ese desempeño',
		reviewStatus: 'En revisión',
	},
	{
		title: 'Cuando las Soluciones se Vuelven el Sistema',
		date: 'Agosto de 2026',
		status: 'Enviado',
		description:
			'Cómo soluciones exitosas pueden hacer que una tecnología existente sea progresivamente más difícil de reemplazar',
		reviewStatus: 'En revisión',
	},
];
---

<!doctype html>

<html lang="es">
	<head>
		<script is:inline>
			document.documentElement.classList.add('js');
		</script>

		<SEO
			title="Papers | Fede Rodrigo"
			description="Papers sobre liderazgo tecnológico, dependencia organizacional, lock-in tecnológico y toma de decisiones"
			lang="es"
		/>
	</head>

	<body>
		<Header />

		<main>
			<section class="hero">
				<h1 class="hero-title">
					<span class="hero-line">
						Papers <span class="accent">Seleccionados</span>
					</span>
				</h1>

				<div class="lead-principles">
					<div>Tecnología</div>
					<div>Organizaciones</div>
					<div>Liderazgo</div>
				</div>
			</section>

			{
				papers.map((paper) => (
					<section class="contact-section reveal-section">
						<div class="reveal-item">
							<p class="books-page-label">
								{paper.status} · {paper.date}
							</p>

							<h2>{paper.title}</h2>

							<p class="paper-description">
								{paper.description}
							</p>

							<p class="paper-review-status">
								{paper.reviewStatus}
							</p>
						</div>
					</section>
				))
			}
		</main>

		<Footer />

		<script>
			const sections =
				document.querySelectorAll('.reveal-section');

			const revealObserver = new IntersectionObserver(
				(entries, observer) => {
					entries.forEach((entry) => {
						if (!entry.isIntersecting) return;

						entry.target.classList.add('is-visible');
						observer.unobserve(entry.target);
					});
				},
				{
					threshold: 0.08,
					rootMargin: '0px 0px -50px 0px',
				}
			);

			sections.forEach((section) => {
				revealObserver.observe(section);
			});
		</script>

		<style>
	.paper-description {
		margin-bottom: 0;
	}
</style>
	</body>
</html>
