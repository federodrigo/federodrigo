---
import SEO from '../components/SEO.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

type MediaItem = {
	type: string;
	title: string;
	date: string;
	image: string;
	imageAlt: string;
	url?: string;
	linkLabel?: string;
	pdfUrl?: string;
	pdfLabel?: string;
	internal?: boolean;
	imageFit?: 'cover' | 'contain';
	imagePosition?: string;
	imageClass?: string;
};

const mediaItems: MediaItem[] = [
	{
		type: 'Webinar',
		title: 'Technology is Never the Starting Point — Xseed',
		date: 'August 2026',
		image: '/images/xseed-webinar.png',
		imageAlt:
			'Technology is Never the Starting Point webinar with Federico Rodrigo',
		url: '/webinar/',
		linkLabel: 'Explore the talk',
		internal: true,
		imageFit: 'contain',
		imagePosition: 'center center'
	},
	{
		type: 'Podcast',
		title: 'The Technology Vanguard',
		date: 'August 2026',
		image: '/images/vanguard.png',
		imageAlt:
			'Fede Rodrigo appearing on The Technology Vanguard podcast',
		url: 'https://www.youtube.com/watch?v=2wyTHhaOcWE',
		linkLabel: 'Watch the conversation',
		imageFit: 'contain',
		imagePosition: 'center center',
		imageClass: 'media-image-vanguard'
	},
	{
		type: 'Interview',
		title: 'Cloud Investment & Business Value — Nublit',
		date: 'January 2026',
		image: '/images/cloud-interview.jpg',
		imageAlt:
			'Interview with Fede Rodrigo published by Nublit',
		url: 'https://informe2026.nublit.com/',
		linkLabel: 'View full report',
		pdfUrl: '/pdfs/Fede-Rodrigo-Interview-Nublit.pdf',
		pdfLabel: 'Read my interview',
		imageFit: 'contain',
		imagePosition: 'center center',
		imageClass: 'media-image-nublit'
	},
	{
		type: 'Executive Roundtable',
		title: 'Industrial Reinvention — The Economist',
		date: 'September 2024',
		image: '/images/impact.png',
		imageAlt:
			'Fede Rodrigo participating in The Economist Impact executive roundtable',
		imageFit: 'contain',
		imagePosition: 'center center',
		imageClass: 'media-image-economist'
	}
];

const recognition: MediaItem = {
	type: 'Recognition',
	title: 'ORBIE CIO Awards',
	date: 'September 2024',
	image: '/images/orbie.jpg',
	imageAlt: 'ORBIE Awards CIO Central Texas',
	url: 'https://orbie.org/honoree/federico-rodrigo/4324',
	linkLabel: 'View recognition',
	imageFit: 'cover',
	imagePosition: 'center 42%'
};
---

<!doctype html>

<html lang="en">
	<head>
		<script is:inline>
			document.documentElement.classList.add('js');
		</script>

		<SEO
			title="Speaking | Fede Rodrigo"
			description="Speaking engagements, webinars, executive sessions, podcasts, interviews, and conversations on technology, business, leadership, and execution"
			lang="en"
		/>
	</head>

	<body>
		<Header />

		<main>
			<!-- SPEAKING HERO -->

			<section class="hero speaking-hero">
				<h1 class="hero-title speaking-hero-title">
					<span class="hero-line">What if your next technology</span>
					<span class="hero-line">decision started with</span>
					<span class="hero-line"><span class="speaking-hero-accent">no technology at all?</span></span>
				</h1>
			</section>

			<!-- CENTRAL IDEA -->

			<section
				class="speaking-thesis reveal-section"
				aria-labelledby="speaking-thesis-title"
			>
				<div class="speaking-thesis-copy">
					<p class="speaking-thesis-text reveal-item">
						Technology decisions should begin with the business
						problem, the people who have to make the change work,
						and the outcome the organization is trying to create
					</p>

					<h2
						id="speaking-thesis-title"
						class="speaking-thesis-conclusion reveal-item reveal-delay-1"
					>
						Technology is <span class="speaking-never">never</span> the starting point
					</h2>

					<p class="speaking-thesis-link reveal-item reveal-delay-1">
						<a href="/webinar/" class="animated-link">
							<span>Explore the talk</span>

							<span
								class="animated-link-arrow"
								aria-hidden="true"
							>
								→
							</span>
						</a>
					</p>
				</div>
			</section>

			<!-- SPEAKING SLIDES -->

			<section
				class="speaking-showcase reveal-section"
				aria-label="Speaking presentation slides"
			>
				<div
					class="speaking-slider reveal-item"
					data-speaking-slider
				>
					<div class="speaking-slider-stage">
						<figure
							class="speaking-slide is-active"
							data-speaking-slide
							aria-hidden="false"
						>
							<img
								class="speaking-slide-image"
								src="/images/speaking-slide-question-clean.png"
								alt="What if your next technology decision started with no technology at all?"
								loading="eager"
							/>
						</figure>

						<figure
							class="speaking-slide"
							data-speaking-slide
							aria-hidden="true"
						>
							<img
								class="speaking-slide-image"
								src="/images/speaking-slide-business-value.png"
								alt="Why does IT exist? To create business value"
								loading="lazy"
							/>
						</figure>

						<figure
							class="speaking-slide"
							data-speaking-slide
							aria-hidden="true"
						>
							<img
								class="speaking-slide-image"
								src="/images/speaking-slide-leadership.png"
								alt="Technology will always change. Leadership will not. Technology is never the starting point"
								loading="lazy"
							/>
						</figure>
					</div>

					<div
						class="speaking-slider-nav"
						aria-label="Choose slide"
					>
						<button
							type="button"
							class="speaking-slider-dot is-active"
							data-speaking-dot="0"
							aria-label="Show slide 1"
							aria-current="true"
						></button>

						<button
							type="button"
							class="speaking-slider-dot"
							data-speaking-dot="1"
							aria-label="Show slide 2"
							aria-current="false"
						></button>

						<button
							type="button"
							class="speaking-slider-dot"
							data-speaking-dot="2"
							aria-label="Show slide 3"
							aria-current="false"
						></button>
					</div>
				</div>
			</section>

			<div
				class="speaking-divider"
				aria-hidden="true"
			></div>

			<!-- SELECTED APPEARANCES -->

			<section
				class="media-events reveal-section"
				aria-labelledby="media-events-title"
			>
				<h2
					id="media-events-title"
					class="reveal-item"
				>
					Selected Appearances
				</h2>

				<div class="media-grid">
					{
						mediaItems.map((item, index) => (
							<article
								class:list={[
									'media-card',
									'reveal-item',
									{
										'media-card--nublit':
											Boolean(item.pdfUrl)
									}
								]}
								style={`
									--delay: ${index * 70}ms;
									--media-fit: ${item.imageFit ?? 'cover'};
									--media-position: ${
										item.imagePosition ?? 'center center'
									};
								`}
							>
								<div class="media-card-image">
									{item.url ? (
										<a
											href={item.url}
											target={
												item.internal
													? undefined
													: '_blank'
											}
											rel={
												item.internal
													? undefined
													: 'noopener noreferrer'
											}
											aria-label={
												item.linkLabel ??
												item.title
											}
										>
											<img
												class:list={[
													'media-card-image-element',
													item.imageClass
												]}
												src={item.image}
												alt={item.imageAlt}
												loading="lazy"
											/>
										</a>
									) : (
										<img
											class:list={[
												'media-card-image-element',
												item.imageClass
											]}
											src={item.image}
											alt={item.imageAlt}
											loading="lazy"
										/>
									)}
								</div>

								<div class="media-card-content">
									<p class="media-card-meta">
										<span>{item.type}</span>

										<span aria-hidden="true">·</span>

										<span>{item.date}</span>
									</p>

									<h3>{item.title}</h3>

									{(item.url || item.pdfUrl) && (
										<div class="media-card-actions">
											{item.pdfUrl &&
												item.pdfLabel && (
													<a
														href={
															item.pdfUrl
														}
														target="_blank"
														rel="noopener noreferrer"
														class="animated-link"
													>
														<span>
															{
																item.pdfLabel
															}
														</span>

														<span
															class="animated-link-arrow"
															aria-hidden="true"
														>
															→
														</span>
													</a>
												)}

											{item.url && (
												<a
													href={item.url}
													target={
														item.internal
															? undefined
															: '_blank'
													}
													rel={
														item.internal
															? undefined
															: 'noopener noreferrer'
													}
													class="animated-link"
												>
													<span>
														{
															item.linkLabel ??
																'Learn more'
														}
													</span>

													<span
														class="animated-link-arrow"
														aria-hidden="true"
													>
														→
													</span>
												</a>
											)}
										</div>
									)}
								</div>
							</article>
						))
					}
				</div>

				<!-- RECOGNITION -->

				<article
					class="recognition-card reveal-item"
					style={`
						--delay: ${mediaItems.length * 70}ms;
						--media-fit: ${recognition.imageFit ?? 'cover'};
						--media-position: ${
							recognition.imagePosition ?? 'center center'
						};
					`}
				>
					<div class="recognition-image">
						<a
							href={recognition.url}
							target="_blank"
							rel="noopener noreferrer"
							aria-label={recognition.linkLabel}
						>
							<img
								src={recognition.image}
								alt={recognition.imageAlt}
								loading="lazy"
							/>
						</a>
					</div>

					<div class="recognition-content">
						<p class="media-card-meta">
							<span>{recognition.type}</span>

							<span aria-hidden="true">·</span>

							<span>{recognition.date}</span>
						</p>

						<h3>{recognition.title}</h3>

						<a
							href={recognition.url}
							target="_blank"
							rel="noopener noreferrer"
							class="animated-link recognition-link"
						>
							<span>{recognition.linkLabel}</span>

							<span
								class="animated-link-arrow"
								aria-hidden="true"
							>
								→
							</span>
						</a>
					</div>
				</article>
			</section>

			<!-- SPEAKING INQUIRIES -->

			<section class="cio-office-contact reveal-section">
				<h2 class="reveal-item">
					Speaking Inquiries
				</h2>

				<div class="reveal-item reveal-delay-1">
					<p>
						For speaking engagements, interviews, podcasts,
						or other media opportunities, please get in touch
					</p>

					<p class="cio-office-contact-link">
						<a
							href="/contact/"
							class="animated-link"
						>
							<span>Contact me</span>

							<span
								class="animated-link-arrow"
								aria-hidden="true"
							>
								→
							</span>
						</a>
					</p>
				</div>
			</section>
		</main>

		<Footer />

		<!-- REVEAL -->

		<script>
			const sections =
				document.querySelectorAll(
					'.reveal-section'
				);

			if ('IntersectionObserver' in window) {
				const revealObserver =
					new IntersectionObserver(
						(entries, observer) => {
							entries.forEach((entry) => {
								if (!entry.isIntersecting) {
									return;
								}

								entry.target.classList.add(
									'is-visible'
								);

								observer.unobserve(
									entry.target
								);
							});
						},
						{
							threshold: 0.08,
							rootMargin:
								'0px 0px -50px 0px'
						}
					);

				sections.forEach((section) => {
					revealObserver.observe(section);
				});
			} else {
				sections.forEach((section) => {
					section.classList.add(
						'is-visible'
					);
				});
			}
		</script>

		<!-- SLIDER -->

		<script>
			const speakingSlider =
				document.querySelector(
					'[data-speaking-slider]'
				);

			if (speakingSlider) {
				const slides = Array.from(
					speakingSlider.querySelectorAll(
						'[data-speaking-slide]'
					)
				);

				const dots = Array.from(
					speakingSlider.querySelectorAll(
						'[data-speaking-dot]'
					)
				);

				const reducedMotion =
					window.matchMedia(
						'(prefers-reduced-motion: reduce)'
					).matches;

				let currentSlide = 0;
				let slideTimer = null;
				let sliderVisible = false;

				const durations = [
					7000,
					5200,
					5200
				];

				function showSlide(index) {
					currentSlide =
						(index + slides.length) %
						slides.length;

					slides.forEach(
						(slide, slideIndex) => {
							const active =
								slideIndex ===
								currentSlide;

							slide.classList.toggle(
								'is-active',
								active
							);

							slide.setAttribute(
								'aria-hidden',
								active
									? 'false'
									: 'true'
							);
						}
					);

					dots.forEach(
						(dot, dotIndex) => {
							const active =
								dotIndex ===
								currentSlide;

							dot.classList.toggle(
								'is-active',
								active
							);

							dot.setAttribute(
								'aria-current',
								active
									? 'true'
									: 'false'
							);
						}
					);
				}

				function stopSlider() {
					if (!slideTimer) {
						return;
					}

					window.clearTimeout(slideTimer);
					slideTimer = null;
				}

				function scheduleNextSlide() {
					if (
						reducedMotion ||
						!sliderVisible
					) {
						return;
					}

					stopSlider();

					slideTimer =
						window.setTimeout(() => {
							showSlide(
								currentSlide + 1
							);

							scheduleNextSlide();
						}, durations[currentSlide]);
				}

				dots.forEach((dot, index) => {
					dot.addEventListener(
						'click',
						() => {
							showSlide(index);
							scheduleNextSlide();
						}
					);
				});

				speakingSlider.addEventListener(
					'mouseenter',
					stopSlider
				);

				speakingSlider.addEventListener(
					'mouseleave',
					scheduleNextSlide
				);

				if ('IntersectionObserver' in window) {
					const sliderObserver =
						new IntersectionObserver(
							(entries) => {
								entries.forEach((entry) => {
									sliderVisible =
										entry.isIntersecting;

									if (sliderVisible) {
										scheduleNextSlide();
									} else {
										stopSlider();
									}
								});
							},
							{
								threshold: 0.35
							}
						);

					sliderObserver.observe(
						speakingSlider
					);
				} else {
					sliderVisible = true;
					scheduleNextSlide();
				}

				document.addEventListener(
					'visibilitychange',
					() => {
						if (document.hidden) {
							stopSlider();
						} else {
							scheduleNextSlide();
						}
					}
				);
			}
		</script>
	</body>
</html>

<style>
	/* =========================================================
	   SPEAKING HERO — MINIMAL PAGE-SPECIFIC OVERRIDES
	========================================================= */

	.speaking-hero {
		border-bottom: 0 !important;
	}

	.speaking-hero-title {
		width: 100%;
		max-width: 1080px;
		margin-left: auto;
		margin-right: auto;
		font-size: clamp(2rem, 3.65vw, 3.75rem);
		line-height: 1.08;
		text-align: center;
	}

	.speaking-hero-title .hero-line {
		width: 100%;
		white-space: nowrap;
	}

	.speaking-hero-accent,
	.speaking-never {
		color: #6fa7ef;
	}

	.speaking-hero-accent {
		display: inline-block;
		opacity: 0;
		transform: translateY(6px);
		animation: speakingAccentReveal 520ms ease 720ms forwards;
	}

	@keyframes speakingAccentReveal {
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	.speaking-thesis-link {
		margin: 20px 0 0;
	}

	/* =========================================================
	   CENTRAL IDEA
	========================================================= */

	.speaking-thesis {
		width: min(1120px, calc(100% - 48px));
		margin: 8px auto 42px;
		padding: 0;
		border-top: 0 !important;
	}

	.speaking-thesis-copy {
		max-width: 880px;
		margin: 0 auto;
		text-align: center;
	}

	.speaking-thesis-text {
		max-width: 760px;
		margin: 0 auto 18px;
		color: #6f7d8f;
		font-size: clamp(1.05rem, 1.35vw, 1.22rem);
		line-height: 1.58;
		letter-spacing: -0.015em;
	}

	.speaking-thesis-conclusion {
		max-width: 900px;
		margin: 0 auto;
		color: #102b46;
		font-size: clamp(1.5rem, 2.1vw, 2rem);
		font-weight: 650;
		line-height: 1.22;
		letter-spacing: -0.035em;
	}

	.speaking-thesis-conclusion::before,
	.speaking-thesis-conclusion::after {
		display: none !important;
		content: none !important;
	}

	/* =========================================================
	   SPEAKING SLIDER
	========================================================= */

	.speaking-showcase {
		width: min(980px, calc(100% - 48px));
		margin: 0 auto 4px;
	}

	.speaking-slider {
		width: 100%;
	}

	.speaking-slider-stage {
		position: relative;
		width: 100%;
		aspect-ratio: 16 / 9;
		overflow: hidden;
		border: 1px solid rgba(16, 43, 70, 0.06);
		border-radius: 14px;
		background: #ffffff;
		box-shadow:
			0 16px 40px
			rgba(16, 43, 70, 0.065);
	}

	.speaking-slide {
		position: absolute;
		inset: clamp(24px, 3vw, 42px);
		display: flex;
		align-items: center;
		justify-content: center;
		margin: 0;
		opacity: 0;
		pointer-events: none;
		transition: opacity 650ms ease;
	}

	.speaking-slide.is-active {
		z-index: 2;
		opacity: 1;
		pointer-events: auto;
	}

	.speaking-slide-image {
		display: block !important;
		width: 100% !important;
		height: 100% !important;
		max-width: 100% !important;
		max-height: 100% !important;
		object-fit: contain !important;
		object-position: center center !important;
		background: #ffffff;
	}

	/* =========================================================
	   SLIDER DOTS
	========================================================= */

	.speaking-slider-nav {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 8px;
		height: 30px;
		margin-top: 5px;
	}

	.speaking-slider-dot {
		width: 7px;
		height: 7px;
		padding: 0;
		border: 0;
		border-radius: 50%;
		background: rgba(16, 43, 70, 0.23);
		cursor: pointer;
		transition:
			width 200ms ease,
			background 200ms ease,
			border-radius 200ms ease;
	}

	.speaking-slider-dot.is-active {
		width: 23px;
		border-radius: 999px;
		background: #102b46;
	}

	.speaking-slider-dot:focus-visible {
		outline: 2px solid #6fa7ef;
		outline-offset: 4px;
	}


	.speaking-divider {
		width: min(1120px, calc(100% - 48px));
		height: 1px;
		margin: 28px auto 34px;
		background: rgba(16, 43, 70, 0.12);
	}

	/* =========================================================
	   SELECTED APPEARANCES
	========================================================= */

	.media-events {
		width: min(1120px, calc(100% - 48px));
		margin: 0 auto 22px !important;
		padding: 0 !important;
	}

	.media-events > h2 {
		margin: 0 0 22px;
	}

	.media-grid {
		display: grid;
		grid-template-columns:
			repeat(2, minmax(0, 1fr));
		gap: 18px;
	}

	/* =========================================================
	   MEDIA CARDS
	========================================================= */

	.media-card {
		display: flex;
		flex-direction: column;
		width: 100%;
		height: 350px;
		min-width: 0;
		overflow: hidden;
		border: 1px solid rgba(16, 43, 70, 0.1);
		border-radius: 12px;
		background: #ffffff;
		transition:
			transform 200ms ease,
			box-shadow 200ms ease;
	}

	.media-card:hover,
	.recognition-card:hover {
		transform: translateY(-2px);
		box-shadow:
			0 12px 28px
			rgba(16, 43, 70, 0.06);
	}

	.media-card-image {
		flex: 0 0 205px;
		width: 100%;
		height: 205px;
		overflow: hidden;
		border-bottom:
			1px solid
			rgba(16, 43, 70, 0.08);
		background: #f4f5f7;
	}

	.media-card-image a {
		display: block;
		width: 100%;
		height: 100%;
	}

	.media-card-image-element {
		display: block !important;
		width: 100% !important;
		height: 100% !important;
		max-width: none !important;
		max-height: none !important;
		object-fit: var(--media-fit, cover) !important;
		object-position:
			var(--media-position, center center) !important;
		transform-origin: center center;
	}

	.media-image-vanguard {
		object-fit: contain !important;
		object-position: center center !important;
		transform: scale(0.96) !important;
	}

	.media-image-nublit {
		object-fit: contain !important;
		object-position: center center !important;
		transform: scale(1.22) !important;
	}

	.media-image-economist {
		object-fit: contain !important;
		object-position: center center !important;
		transform: scale(0.96) !important;
	}

	.media-card-content {
		display: flex;
		flex: 1;
		flex-direction: column;
		min-height: 0;
		padding: 16px 18px 17px;
	}

	.media-card-meta {
		display: flex;
		flex-wrap: wrap;
		gap: 4px;
		margin: 0 0 7px;
		color: #7d8b9a;
		font-size: 0.68rem;
		font-weight: 700;
		line-height: 1.3;
		letter-spacing: 0.055em;
		text-transform: uppercase;
	}

	.media-card h3 {
		display: -webkit-box;
		margin: 0;
		overflow: hidden;
		-webkit-box-orient: vertical;
		-webkit-line-clamp: 2;
		color: #102b46;
		font-size: clamp(1rem, 1.15vw, 1.12rem);
		font-weight: 600;
		line-height: 1.2;
		letter-spacing: -0.02em;
	}

	.media-card-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 6px 12px;
		margin-top: auto;
		padding-top: 10px;
	}

	.media-card-actions .animated-link {
		font-size: 0.8rem;
	}

	/* =========================================================
	   RECOGNITION
	========================================================= */

	.recognition-card {
		display: grid;
		grid-template-columns: 210px minmax(0, 1fr);
		min-height: 124px;
		margin-top: 16px;
		overflow: hidden;
		border: 1px solid rgba(16, 43, 70, 0.1);
		border-radius: 12px;
		background: #ffffff;
		transition:
			transform 200ms ease,
			box-shadow 200ms ease;
	}

	.recognition-image {
		width: 210px;
		min-height: 124px;
		overflow: hidden;
		border-right:
			1px solid
			rgba(16, 43, 70, 0.08);
		background: #f4f5f7;
	}

	.recognition-image a {
		display: block;
		width: 100%;
		height: 100%;
	}

	.recognition-image img {
		display: block;
		width: 100%;
		height: 100%;
		object-fit: var(--media-fit, cover);
		object-position:
			var(--media-position, center center);
	}

	.recognition-content {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		justify-content: center;
		padding: 17px 20px;
	}

	.recognition-card h3 {
		margin: 0;
		color: #102b46;
		font-size: 1.08rem;
		font-weight: 600;
		line-height: 1.2;
		letter-spacing: -0.02em;
	}

	.recognition-link {
		margin-top: 10px;
		font-size: 0.78rem;
	}

	/* =========================================================
	   REVEALS
	========================================================= */

	.js .reveal-section .reveal-item {
		opacity: 0;
		transform: translateY(14px);
		transition:
			opacity 600ms ease,
			transform 600ms
				cubic-bezier(0.22, 1, 0.36, 1);
		transition-delay: var(--delay, 0ms);
	}

	.js .reveal-section.is-visible .reveal-item {
		opacity: 1;
		transform: translateY(0);
	}

	.js .reveal-delay-1 {
		transition-delay: 90ms !important;
	}

	/* =========================================================
	   TABLET
	========================================================= */

	@media (max-width: 1020px) {
		.speaking-showcase {
			width: min(860px, calc(100% - 48px));
		}

		.media-grid {
			grid-template-columns:
				repeat(2, minmax(0, 1fr));
			gap: 16px;
		}

		.media-card {
			height: 330px;
		}

		.media-card-image {
			flex-basis: 180px;
			height: 180px;
		}

		.media-card-content {
			padding: 14px 16px 15px;
		}

		.media-card-meta {
			font-size: 0.68rem;
		}

		.media-card h3 {
			font-size: 1.05rem;
		}

		.media-card-actions .animated-link {
			font-size: 0.8rem;
		}
	}

	/* =========================================================
	   MOBILE
	========================================================= */

	@media (max-width: 620px) {
		.speaking-hero-title {
			max-width: 100%;
			font-size: clamp(1.95rem, 7.8vw, 2.65rem);
			line-height: 1.1;
		}

		.speaking-hero-title .hero-line {
			white-space: normal;
		}

		.speaking-thesis,
		.speaking-showcase,
		.speaking-divider,
		.media-events {
			width: calc(100% - 32px);
		}

		.speaking-thesis {
			margin-top: 18px;
			margin-bottom: 25px;
		}

		.speaking-thesis-text {
			margin-bottom: 14px;
			font-size: 1rem;
			line-height: 1.58;
		}

		.speaking-thesis-conclusion {
			font-size: 1.45rem;
		}

		.speaking-slider-stage {
			border-radius: 10px;
		}

		.speaking-slide {
			inset: 15px;
		}

		.speaking-slider-nav {
			height: 28px;
			margin-top: 4px;
		}

		.speaking-divider {
			margin: 24px auto 28px;
		}

		.media-events {
			margin-bottom: 18px !important;
		}

		.media-events > h2 {
			margin-bottom: 19px;
		}

		.media-grid {
			grid-template-columns: 1fr;
			gap: 16px;
		}

		.media-card {
			height: 295px;
		}

		.media-card-image {
			flex-basis: 145px;
			height: 145px;
		}

		.media-card-content {
			padding: 13px 16px 14px;
		}

		.media-card-actions {
			margin-top: 12px;
			padding-top: 0;
		}

		.recognition-card {
			grid-template-columns: 1fr;
			margin-top: 16px;
		}

		.recognition-image {
			width: 100%;
			height: 158px;
			min-height: 158px;
			border-right: 0;
			border-bottom:
				1px solid
				rgba(16, 43, 70, 0.08);
		}

		.recognition-content {
			padding: 15px 16px 16px;
		}
	}

	/* =========================================================
	   REDUCED MOTION
	========================================================= */

	@media (prefers-reduced-motion: reduce) {
		.speaking-hero-accent {
			opacity: 1;
			transform: none;
			animation: none !important;
		}

		.speaking-slide,
		.speaking-slider-dot,
		.media-card,
		.recognition-card,
		.reveal-item {
			transition: none !important;
		}
	}
</style>
